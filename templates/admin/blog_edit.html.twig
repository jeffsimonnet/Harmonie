{% extends 'admin/index.html.twig' %}

{% block title %}Edition Article{% endblock %}

{% block content %}
    <div class="bloc | grid-span-12-col">
        <div class="heading">
            <h3>Edition Article</h3>
            {% if page.id %}<a href="{{ path("admin_page_preview", {page : page.id}) }}" class="btn_admin icon add" target="_blank">Prévisualiser <span>l'article</span></a>{% endif %}
            <a href="{{ path('admin_blog_index') }}">Retour <span>à la liste</span> &#8634;</a>
        </div>
        <div class="content">
            {{ include('_document/page/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
