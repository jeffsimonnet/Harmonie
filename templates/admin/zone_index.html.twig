{% extends 'admin/index.html.twig' %}

{% use "_dataTables/_table.html.twig" %}

{% block title %}Gestion des Zones géographiques{% endblock %}

{% block content %}
    <div class="bloc | grid-span-12-col | page_index">
        <div class="heading">
            <h3>Les Zones géographiques</h3>
            <a href="{{ path('admin_zone_new') }}" class="btn_admin icon add">Ajouter <span>une zone</span></a>
        </div>
        <div class="content dataTable">
            Chargement...
        </div>
        {{ block('table_tips') }}
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ block('table_init_script_noselect') }}
{% endblock %}